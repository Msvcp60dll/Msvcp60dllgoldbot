# PRODUCTION ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your values

# === REQUIRED VARIABLES ===

# Telegram Bot Token from @BotFather
BOT_TOKEN=your_bot_token_here

# PostgreSQL Database URL (get from Railway or Supabase)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Telegram Group/Channel ID (negative number for groups)
# Get this by adding @userinfobot to your group
GROUP_ID=-1001234567890

# Webhook Secret (generate a random 32+ character string)
# Used to verify webhook requests from Telegram
WEBHOOK_SECRET=generate_random_32_char_string_here

# === OPTIONAL BUT RECOMMENDED ===

# Your domain for webhook (Railway provides this)
# Example: https://your-app.up.railway.app
WEBHOOK_URL=https://your-app.up.railway.app

# Bot Owner ID (your Telegram user ID)
# The bot will send daily stats to this user
BOT_OWNER_ID=your_telegram_id

# Dashboard Admin Tokens (comma-separated)
# Generate secure tokens for accessing /admin/dashboard
DASHBOARD_TOKENS=token1,token2,token3

# === PAYMENT CONFIGURATION ===

# Subscription price in Telegram Stars (default: 30 for testing, use 2500 for production)
SUB_STARS=30

# One-time payment price in Telegram Stars (default: 30 for testing, use 3800 for production)
PLAN_STARS=30

# Subscription duration in days (default: 30)
PLAN_DAYS=30

# Grace period in hours after expiration (default: 24)
GRACE_PERIOD_HOURS=24

# === ENVIRONMENT SETTINGS ===

# Environment name (production/staging/development)
ENVIRONMENT=production

# Log level (INFO/WARNING/ERROR/DEBUG)
LOG_LEVEL=INFO

# Port for web server (Railway sets this automatically)
# PORT=8080

# === SECURITY ===

# Telegram API Secret Token (optional but recommended)
# Set this in @BotFather with /setwebhooksecret
TELEGRAM_SECRET=optional_telegram_secret

# === FEATURE FLAGS ===

# Enable subscription link tracking (true/false)
ENABLE_TRACKING=true

# Enable auto-approval for active subscriptions (true/false)
AUTO_APPROVE=true

# === NOTES ===
# 1. Never commit this file with real values
# 2. Use strong, unique passwords and secrets
# 3. Railway automatically provides PORT variable
# 4. Database migrations run automatically on startup
# 5. Health check available at /health endpoint